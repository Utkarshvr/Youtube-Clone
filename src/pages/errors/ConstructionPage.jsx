import { Typography, Box } from "@mui/material";

export default function ConstructionPage() {
  return (
    <Box
      width="100%"
      height="92vh"
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Typography fontSize={32} component="span" color="Background">
        Out Soon in The Next Version
      </Typography>
    </Box>
  );
}
